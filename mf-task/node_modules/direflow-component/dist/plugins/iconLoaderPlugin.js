const iconLoaderPlugin = (element, plugins, app) => {
    var _a, _b, _c;
    const plugin = (_a = plugins) === null || _a === void 0 ? void 0 : _a.find((p) => p.name === 'icon-loader');
    if (!app) {
        return;
    }
    if ((_c = (_b = plugin) === null || _b === void 0 ? void 0 : _b.options) === null || _c === void 0 ? void 0 : _c.packs.includes('material-icons')) {
        const link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://fonts.googleapis.com/icon?family=Material+Icons';
        const insertionPoint = document.createElement('span');
        insertionPoint.id = 'direflow_material-icons';
        insertionPoint.appendChild(link);
        return [app, insertionPoint];
    }
};
export default iconLoaderPlugin;
