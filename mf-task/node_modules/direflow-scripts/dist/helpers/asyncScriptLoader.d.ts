declare type TBundle = {
    script: Element;
    hasLoaded: boolean;
};
declare global {
    interface Window {
        wcPolyfillsLoaded: TBundle[];
        reactBundleLoaded: TBundle[];
    }
}
declare const asyncScriptLoader: (src: string, bundleListKey: "wcPolyfillsLoaded" | "reactBundleLoaded") => Promise<void>;
export default asyncScriptLoader;
