"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var fs_1 = __importDefault(require("fs"));
var path_1 = require("path");
var handlebars_1 = __importDefault(require("handlebars"));
var DEFAULT_REACT = 'https://unpkg.com/react@16/umd/react.production.min.js';
var DEFAULT_REACT_DOM = 'https://unpkg.com/react-dom@16/umd/react-dom.production.min.js';
function entryResolver(indexPath, _a) {
    var react = _a.react, reactDOM = _a.reactDOM;
    var paths = indexPath.split(path_1.sep);
    var folderPath = __spreadArrays(paths).slice(0, paths.length - 1).join(path_1.sep);
    var reactResource = 'none';
    var reactDOMResource = 'none';
    if (react !== false) {
        reactResource = react || DEFAULT_REACT;
    }
    if (reactDOM !== false) {
        reactDOMResource = reactDOM || DEFAULT_REACT_DOM;
    }
    var entryLoaderFile = fs_1.default.readFileSync(path_1.resolve(__dirname, '../template-scripts/entryLoader.js'), 'utf8');
    var componentFolders = fs_1.default.readdirSync(path_1.join(folderPath, 'direflow-components'));
    var entryLoaderTemplate = handlebars_1.default.compile(entryLoaderFile);
    var mainEntryFile = entryLoaderTemplate({
        pathIndex: path_1.join(folderPath, paths[paths.length - 1]),
        reactResource: reactResource,
        reactDOMResource: reactDOMResource,
    });
    var mainEntryLoaderPath = path_1.resolve(__dirname, '../main.js');
    fs_1.default.writeFileSync(mainEntryLoaderPath, mainEntryFile);
    var entryList = componentFolders
        .map(function (folder) {
        var _a;
        if (!fs_1.default.statSync(path_1.join(folderPath, 'direflow-components', folder)).isDirectory()) {
            return;
        }
        var pathIndex = path_1.join(folderPath, 'direflow-components', folder, paths[paths.length - 1]);
        if (!fs_1.default.existsSync(pathIndex)) {
            return;
        }
        var escapedPathIndex = pathIndex.replace(/\\/g, '\\\\');
        var entryFile = entryLoaderTemplate({ pathIndex: escapedPathIndex, reactResource: reactResource, reactDOMResource: reactDOMResource });
        var entryLoaderPath = path_1.resolve(__dirname, "../" + folder + ".js");
        fs_1.default.writeFileSync(entryLoaderPath, entryFile);
        return _a = {}, _a[folder] = entryLoaderPath, _a;
    })
        .filter(Boolean);
    entryList.unshift({ main: mainEntryLoaderPath });
    return entryList;
}
exports.default = entryResolver;
