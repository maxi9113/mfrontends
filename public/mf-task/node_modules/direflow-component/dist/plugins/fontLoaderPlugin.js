import WebFont from 'webfontloader';
let didInclude = false;
const fontLoaderPlugin = (element, plugins) => {
    var _a, _b;
    if (didInclude) {
        return;
    }
    const plugin = (_a = plugins) === null || _a === void 0 ? void 0 : _a.find((p) => p.name === 'font-loader');
    if ((_b = plugin) === null || _b === void 0 ? void 0 : _b.options) {
        WebFont.load(plugin.options);
        didInclude = true;
    }
};
export default fontLoaderPlugin;
